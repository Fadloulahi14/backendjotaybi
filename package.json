{
  "name": "json-server-app",
  "version": "1.0.0",
  "description": "API mock avec JSON Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
